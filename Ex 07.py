#Make a program that calculates an area of ​​a square, then show twice the area to the user.
fbase = float(input("Put a base of square: "))
fheight = float(input("Put a height of square: "))
res = (fbase * fheight) * 2
print("The double of area is: ", res)